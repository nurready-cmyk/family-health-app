# Фреймворк Telegram-бота (асинхронный)
aiogram>=3.29.1,<4.0.0

# Загрузка переменных окружения
python-dotenv>=1.0.1

# Доступ к Google Sheets (слой БД в MVP, изолирован внутри database/)
gspread>=6.2.1
google-auth>=2.28.1

# HTTP-транспорт для google-auth (AuthorizedSession) — прямые REST-вызовы
# Google Drive API v3 в services/google_drive_upload.py, без тяжёлого
# google-api-python-client
requests>=2.31.0

# Извлечение структуры из текста/фото (gpt-4o-mini для текста, gpt-4o для саммари фото)
openai>=1.12.0

# Локальная бесплатная расшифровка голосовых сообщений (офлайн, без API)
faster-whisper>=1.2.1

