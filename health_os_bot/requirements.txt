# Telegram bot framework (async)
aiogram>=3.4.1,<4.0.0

# Environment variable loading
python-dotenv>=1.0.1

# Google Sheets access (MVP database layer, isolated inside database/)
gspread>=6.0.2
google-auth>=2.28.1

# LLM text/photo structuring (GPT-4o-mini for text, GPT-4o for photo summaries)
openai>=1.12.0

# Local, offline, free speech-to-text for voice messages
faster-whisper>=1.0.1

# Async file downloads (voice notes, photos) from Telegram
aiofiles>=23.2.1

